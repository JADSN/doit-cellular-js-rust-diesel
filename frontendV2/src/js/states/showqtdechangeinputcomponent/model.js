const stateShowQtdeChangeInputComponentModel = async (idElInputText) => {
    debug(`stateShowQtdeChangeInputComponentModel()`)
    const inpName = idElInputText.split('-').pop()
    return inpName
}

